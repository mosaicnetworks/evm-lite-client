{
  "scripts": {
    "clean:cli": "rm -rf CLI/node_modules && rm -rf CLI/package-lock.json",
    "clean:lib": "rm -rf Library/node_modules && rm -rf Library/package-lock.json",
    "build:lib": "cd Library && npm install && cd ..",
    "build:cli": "cd CLI && npm install && npm link && cd .."
  }
}
